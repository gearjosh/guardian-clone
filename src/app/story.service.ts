import { Injectable } from '@angular/core';
import { Story } from './models/story.model';

@Injectable({
  providedIn: 'root'
})
export class StoryService {

  constructor() { }

  getStories() {
    // return stories;
  }
}
